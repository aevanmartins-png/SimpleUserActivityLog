# UserActivityLog
A simple C# utility for tracking user activity and generating human-readable "time ago" status strings.
This project provides a class to store usernames and their last active timestamps. It converts the time elapsed since their last action into a natural language strin
Automatically scales from minutes to years based on how much time has passed.
Supports both Earth time and Mars time
